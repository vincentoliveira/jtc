       {% extends "JtcDefaultBundle::layout.html.twig" %}

       {% block content %}
<div class="container" style="max-width: 950px;background-color: #fff;height:850px;padding: 0 30px;">
           
            <h2>{{ 'jtc.avis.title'|trans }}</h2>
            <br/>
{% for flashMessage in app.session.flashbag.get('info') %}
        <div class="byline">
                {{ flashMessage }}
            </div>
           {% endfor %}
            <form class="form-horizontal" action="{{ path('jtc_livredor')}}" method="post" {{ form_enctype(form) }} >
                                          {{ form_errors(form) }} 
                    <div class="form-group">
                        <div class="col-sm-5">
                                          {{  form_widget(form.nom, { 'attr': {'placeholder': 'Nom', 'class': 'form-control'} }) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-5">
                                          {{  form_widget(form.prenom, { 'attr': {'placeholder': 'Prénom', 'class': 'form-control'} }) }}
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-sm-5">
                                          {{  form_widget(form.email, { 'attr': {'placeholder': 'Email', 'class': 'form-control'} }) }}
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="col-sm-10">
                                          {{  form_widget(form.message, { 'attr': {'placeholder': 'Message','class': 'form-control'} }) }}
                                        </div>
                                    </div>
                                    <div class="form-group">
                                        <div class="col-sm-10">
                                          {{  form_widget(form) }}
                                            </div>
                                        </div>
                                        <input name="register" value="Laissez un message"  class="col-sm-10 btn-mini" type="submit" id="register">
                                    </form>

    {% include 'JtcAnnonceBundle:Default:listeAvis.html.twig' with {'listAvis': listAvis} %}

        {% endblock %}